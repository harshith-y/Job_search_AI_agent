# Railway Deployment Configuration
# https://railway.app

# Procfile for Railway
web: uvicorn api:app --host 0.0.0.0 --port $PORT

# Or use this if you want scheduled jobs:
# web: uvicorn api:app --host 0.0.0.0 --port $PORT
# worker: python schedule_jobs.py
